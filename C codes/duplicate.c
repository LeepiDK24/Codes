#include<stdio.h>
void check(int a[])
{ int count;
  for(int i=0;i<5;i++)
  {
    for(int j=i+1;j<5;j++)
    {
        if(a[i]==a[j])
        count++;
    }
  } 
  printf("%d occured %d times",&a[i],&count);
}
int main()
{  
   int i;
   int a[5] = {25,12,43,12,25};
   printf("The frequency :");

    int check(a[0]);
     void check(a[1]);
      void check(a[2]);
       void check(a[3]);
        void check(a[4]);
        return 0;
}